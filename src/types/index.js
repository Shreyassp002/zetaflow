/**
 * @fileoverview Type definitions index file
 * Re-exports all type definitions for easy importing
 */

// Import type definitions to make them available
import "./blockchain.js";
import "./zetachain.js";

/**
 * This file serves as the main entry point for all type definitions.
 * Import this file to get access to all JSDoc type definitions:
 *
 * @example
 * import './types/index.js';
 *
 * // Now you can use any of the defined types in JSDoc comments:
 * // @param {TransactionData} transaction
 * // @param {CrossChainTransaction} crossChainTx
 * // @param {GraphData} graphData
 */

export {};
